aiohttp>=3.9
aiosqlite>=0.20
slixmpp>=1.8
aiohttp-socks>=0.9
beautifulsoup4>=4.12
# trafilatura>=1.8  # опционально: лучше извлекает текст статей,
#                   # чем bs4; dogbot/web.py сам падает на bs4/
#                   # regex-фолбэк, если не установлена — можно
#                   # раскомментировать, когда понадобится.
